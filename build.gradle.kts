// Top-level Build-Konfiguration. Plugins werden hier nur deklariert (apply false)
// und in den Modulen (app) angewandt.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
